extends ../admin/layout

block content
    .header
        h1 Choose Your Platform
        p Select a platform to connect and manage your social media channels

    .platforms-grid
        // Threads Platform Card
        .platform-card.threads-card
            .platform-icon
                = '🧵'
            .platform-info
                h2 Threads
                p.platform-desc Meta's text-based social platform. Share updates, engage with your audience, and grow your community.

            .platform-stats
                .platform-stat
                    .stat-label Connected Accounts
                    .stat-value= threadsAccounts.length || 0
                .platform-stat
                    .stat-label Total Posts
                    .stat-value= threadsStats.total || 0
                .platform-stat
                    .stat-label Published
                    .stat-value= threadsStats.published || 0

            .platform-actions
                if threadsAccounts.length > 0
                    a.btn.btn-secondary(href='/admin/accounts') Manage Accounts
                a.btn.btn-primary(href='/login') Add Account

        // Instagram Platform Card (Coming Soon)
        .platform-card.disabled
            .platform-icon
                = '📷'
            .platform-info
                h2 Instagram
                p.platform-desc Coming soon. Connect your Instagram business account to schedule posts, reels, and stories.

            .platform-badge Coming Soon

        // Facebook Platform Card (Coming Soon)
        .platform-card.disabled
            .platform-icon
                = '📘'
            .platform-info
                h2 Facebook
                p.platform-desc Coming soon. Manage your Facebook pages and schedule content across multiple platforms.

            .platform-badge Coming Soon

block scripts
    style.
        .platforms-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: var(--spacing-lg);
            margin: var(--spacing-xl) 0;
        }

        .platform-card {
            position: relative;
            background: var(--bg-secondary);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: var(--radius-xl);
            padding: var(--spacing-xl);
            transition: all var(--transition-normal);
            overflow: hidden;
        }

        .platform-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--accent-primary), var(--accent-secondary));
            opacity: 0;
            transition: opacity var(--transition-normal);
        }

        .platform-card:hover:not(.disabled)::before {
            opacity: 1;
        }

        .platform-card:hover:not(.disabled) {
            transform: translateY(-4px);
            border-color: rgba(204, 255, 0, 0.2);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3), 0 0 40px var(--accent-glow);
        }

        .platform-card.disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        .platform-icon {
            font-size: 48px;
            margin-bottom: var(--spacing-md);
            display: inline-block;
        }

        .platform-info {
            margin-bottom: var(--spacing-lg);
        }

        .platform-info h2 {
            font-family: 'Syne', sans-serif;
            font-size: 28px;
            font-weight: 700;
            margin-bottom: var(--spacing-sm);
            color: var(--text-primary);
        }

        .platform-desc {
            color: var(--text-secondary);
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: var(--spacing-md);
        }

        .platform-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: var(--spacing-md);
            margin-bottom: var(--spacing-lg);
            padding: var(--spacing-md) 0;
            border-top: 1px solid rgba(255, 255, 255, 0.06);
            border-bottom: 1px solid rgba(255, 255, 255, 0.06);
        }

        .platform-stat {
            text-align: center;
        }

        .platform-stat .stat-label {
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--text-tertiary);
        }

        .platform-stat .stat-value {
            font-family: 'Syne', sans-serif;
            font-size: 24px;
            font-weight: 700;
            color: var(--text-primary);
            margin-top: 4px;
        }

        .platform-actions {
            display: flex;
            gap: var(--spacing-sm);
        }

        .platform-badge {
            display: inline-block;
            padding: 6px 12px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: var(--radius-md);
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--text-tertiary);
        }
